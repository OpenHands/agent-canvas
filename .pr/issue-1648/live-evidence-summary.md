# Issue 1648 live evidence

Generated by OpenHands on 2026-07-13 UTC.

No token values, org IDs, conversation IDs, sandbox IDs, session API keys, or
credential-bearing response bodies are included in this artifact. The animated
GIF redacts Cloud device-flow codes and existing account conversation history.

## Immutable inputs

- Issue: https://github.com/OpenHands/agent-canvas/issues/1648
- PR: https://github.com/OpenHands/agent-canvas/pull/1621
- Current `main`: `ed02badba37c3ff7aadd8be3c7499fb71d84a5d8`
- PR head tested before evidence commit: `6641a6740d813b59af4225d9f8aa72cbdd336c4e`
- Branch: `codex/cloud-client-abstraction`
- PR state at re-read: open, non-draft, review required, no reviews, behind current `main`
- PR checks at re-read: CI ubuntu/windows passed, mock-LLM E2E passed, mock-LLM Docker E2E passed, Docker build/push passed, Vercel passed, live-e2e skipped
- npm package version in both worktrees: `@openhands/agent-canvas@1.2.1`
- npm registry latest at evidence time: `@openhands/agent-canvas@1.2.1`
- Production GHCR latest digest at evidence time: `ghcr.io/openhands/agent-canvas:latest@sha256:022976058da2e7f5077621fbec4e4cb160c8c15b96e274fe950937a5e882d6ef`
- PR Docker image from the existing PR body: `ghcr.io/openhands/agent-canvas:sha-6641a67`

## Environment

- OS/workspace: `/workspace/project/agent-canvas`
- Node: `v22.23.1`
- npm: `10.9.8`
- Python: `3.13.13`
- uvx: `0.11.24`
- GitHub CLI: `2.96.0`
- Browser: Playwright Chromium
- GIF tool: Debian `ffmpeg 7.1.5-0+deb13u1` with `LD_LIBRARY_PATH` and `LD_PRELOAD` unset

## Commands

Dependency setup:

```bash
npm ci
(cd /workspace/project/agent-canvas-main && npm ci)
npx playwright install chromium
```

Current main stack:

```bash
env \
  OH_CANVAS_SAFE_STATE_DIR=/tmp/ac1648-main-state \
  OH_SESSION_API_KEY_PATH=/tmp/ac1648-main-state/api-key.txt \
  OH_SECRET_KEY_PATH=/tmp/ac1648-main-state/secret-key.txt \
  OH_AUTOMATION_API_KEY_PATH=/tmp/ac1648-main-state/automation-api-key.txt \
  PORT=12000 \
  OH_CANVAS_SAFE_BACKEND_PORT=18100 \
  OH_CANVAS_SAFE_AUTOMATION_PORT=18101 \
  OH_CANVAS_SAFE_VITE_PORT=13100 \
  VITE_DO_NOT_TRACK=1 \
  npm run dev -- --port 12000
```

PR stack:

```bash
env \
  OH_CANVAS_SAFE_STATE_DIR=/tmp/ac1648-pr-state \
  OH_SESSION_API_KEY_PATH=/tmp/ac1648-pr-state/api-key.txt \
  OH_SECRET_KEY_PATH=/tmp/ac1648-pr-state/secret-key.txt \
  OH_AUTOMATION_API_KEY_PATH=/tmp/ac1648-pr-state/automation-api-key.txt \
  PORT=12001 \
  OH_CANVAS_SAFE_BACKEND_PORT=18200 \
  OH_CANVAS_SAFE_AUTOMATION_PORT=18201 \
  OH_CANVAS_SAFE_VITE_PORT=13101 \
  VITE_DO_NOT_TRACK=1 \
  npm run dev -- --port 12001
```

Browser evidence runner:

```bash
AC1648_BASE_URL=http://127.0.0.1:12000 \
AC1648_LABEL=current-main \
AC1648_SHA=ed02badba37c3ff7aadd8be3c7499fb71d84a5d8 \
AC1648_OUT_DIR=/tmp/ac1648-evidence/main \
OPENHANDS_API_KEY=$OPENHANDS_API_KEY \
node .agent_tmp/issue-1648-evidence-runner.mjs

AC1648_BASE_URL=http://127.0.0.1:12001 \
AC1648_LABEL=pr-head \
AC1648_SHA=6641a6740d813b59af4225d9f8aa72cbdd336c4e \
AC1648_OUT_DIR=/tmp/ac1648-evidence/pr \
OPENHANDS_API_KEY=$OPENHANDS_API_KEY \
node .agent_tmp/issue-1648-evidence-runner.mjs
```

Supplementary runtime proxy probe:

```bash
AC1648_BASE_URL=http://127.0.0.1:12000 \
AC1648_LABEL=current-main \
AC1648_SHA=ed02badba37c3ff7aadd8be3c7499fb71d84a5d8 \
AC1648_OUT_DIR=/tmp/ac1648-evidence/main-proxy \
OPENHANDS_API_KEY=$OPENHANDS_API_KEY \
node .agent_tmp/issue-1648-runtime-proxy-probe.mjs

AC1648_BASE_URL=http://127.0.0.1:12001 \
AC1648_LABEL=pr-head \
AC1648_SHA=6641a6740d813b59af4225d9f8aa72cbdd336c4e \
AC1648_OUT_DIR=/tmp/ac1648-evidence/pr-proxy \
OPENHANDS_API_KEY=$OPENHANDS_API_KEY \
node .agent_tmp/issue-1648-runtime-proxy-probe.mjs
```

GIF build:

```bash
env -u LD_LIBRARY_PATH -u LD_PRELOAD /usr/bin/ffmpeg -y \
  -i /tmp/ac1648-evidence/main/video/page@030934d4e33d616e29e56879c86a6d10.webm \
  -i /tmp/ac1648-evidence/pr/video/page@3ebfad8b1283c1980d43606cceaa90b3.webm \
  -filter_complex "[0:v]fps=8,scale=960:-1:flags=lanczos,setpts=PTS-STARTPTS[v0];[1:v]fps=8,scale=960:-1:flags=lanczos,setpts=PTS-STARTPTS[v1];[v0][v1]concat=n=2:v=1:a=0,drawbox=x=0:y=132:w=225:h=543:color=black@1.0:t=fill,drawbox=x=490:y=326:w=252:h=86:color=black@1.0:t=fill,split[s0][s1];[s0]palettegen=max_colors=128[p];[s1][p]paletteuse=dither=bayer:bayer_scale=5" \
  .pr/issue-1648/issue-1648-main-pr-live-evidence.gif
```

## Stack readiness

- Current main UI/server/automation readiness: `200 / 200 / 200`
- Current main agent-server: `1.35.0`
- Current main automation: health `{"status":"ok"}`
- PR UI/server/automation readiness: `200 / 200 / 200`
- PR agent-server: `1.33.0`
- PR automation: health `{"status":"ok"}`

## Current main observations

- Full UI sequence completed against current `main`.
- Cloud sign-in/device flow was started from the Add Backend modal:
  - `POST https://app.all-hands.dev/oauth/device/authorize` returned `200`.
  - `POST https://app.all-hands.dev/oauth/device/token` returned `400 authorization_pending` while no authenticated browser Cloud session was available.
- Cloud API key/org health through the existing call paths returned `200` for `GET /api/keys/current` and `GET /api/organizations`.
- Conversation startup:
  - First user submission intentionally received a synthetic recoverable `503` for `POST /api/v1/app-conversations`.
  - Retrying the same visible UI path returned `200` for `POST /api/v1/app-conversations`.
  - Start-task polling, app-conversation batch load, and Cloud git changes calls returned `200`.
  - The disposable conversation reached a concrete `/conversations/<id>` route and was deleted with Cloud API `DELETE /api/v1/app-conversations/<id>` returning `200`.
- Client path observed for app-host Cloud calls: XHR/Axios direct to `https://app.all-hands.dev`.
- Supplementary runtime proxy path from the metrics modal:
  - `POST http://127.0.0.1:12000/api/cloud-proxy` was observed.
  - It returned `404` from the local stack.
  - The visible metrics modal opened; the failure did not block the completed conversation startup/recovery evidence.

## PR observations

- The same full UI sequence completed against PR head `6641a6740d813b59af4225d9f8aa72cbdd336c4e`.
- Cloud sign-in/device flow was started from the Add Backend modal:
  - `POST https://app.all-hands.dev/oauth/device/authorize` returned `200`.
  - `POST https://app.all-hands.dev/oauth/device/token` returned `400 authorization_pending` while no authenticated browser Cloud session was available.
- Cloud API key/org health through the PR call paths returned `200` for `GET /api/keys/current` and `GET /api/organizations`.
- Conversation startup:
  - First user submission intentionally received a synthetic recoverable `503` for `POST /api/v1/app-conversations`.
  - Retrying the same visible UI path returned `200` for `POST /api/v1/app-conversations`.
  - Start-task polling, app-conversation batch load, and Cloud git changes calls returned `200`.
  - The disposable conversation reached a concrete `/conversations/<id>` route and was deleted with Cloud API `DELETE /api/v1/app-conversations/<id>` returning `200`.
- Client path observed for app-host Cloud calls: `fetchAndParse -> CloudClient.requestDirect -> CloudClient.request -> callCloudProxy -> service call`.
- Supplementary runtime proxy path from the metrics modal:
  - `POST http://127.0.0.1:12001/api/cloud-proxy` was observed.
  - Browser stack: `fetchAndParse -> CloudClient.requestThroughProxy -> CloudClient.request -> callCloudProxy -> AgentServerConversationService.getRuntimeConversation -> useConversationMetrics`.
  - It returned the same `404` shape as current main from the local stack.
  - This is unchanged baseline behavior, not a PR-only regression.

## Limits and cleanup

- Browser Cloud auth completion was blocked by the absence of an already-authenticated Cloud browser session. The evidence therefore proves real device-flow start and pending poll behavior, not a completed browser login.
- Real Cloud conversation creation used `OPENHANDS_API_KEY` from the environment. The token was never printed or committed.
- All disposable Issue 1648 conversations created by these probes were deleted where the Cloud API supported deletion.
- Raw logs under `/tmp/ac1648-evidence` were not committed because they can contain local browser/account context. The committed JSON is the sanitized subset.
