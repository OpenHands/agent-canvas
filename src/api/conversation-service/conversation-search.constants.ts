/** Page size when scanning the server conversation index for search. */
export const CONVERSATION_SEARCH_PAGE_SIZE = 100;

/** Safety cap when paginating through the full conversation index. */
export const CONVERSATION_SEARCH_MAX_PAGES = 50;

/** Default number of rows shown before the user types a query. */
export const CONVERSATION_SEARCH_RECENT_LIMIT = 50;
